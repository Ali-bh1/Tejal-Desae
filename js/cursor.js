/**
 * cursor.js — Custom cursor is handled entirely via CSS (cursor.css).
 * This file is kept as a no-op so main.js import doesn't break.
 */
export function initCursor() {
  // CSS cursor applied via cursor.css — nothing to do here.
}
